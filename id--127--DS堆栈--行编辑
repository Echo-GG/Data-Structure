#include <iostream>
#include <algorithm>
#include <stack>
using namespace std;
int main()
{
	int t;
	cin >> t;
	while (t--)
	{
		string s;
		cin >> s;
		stack<char>c0;
		string result = "";
		for (char c : s)
		{
			if (c == '#')
			{
				if (!c0.empty())
				{
					c0.pop();
				}
			}
			else
			{
				c0.push(c);
			}
	
		}
		while (!c0.empty())
		{
			result += c0.top();
			c0.pop();
		}
		reverse(result.begin(), result.end());
		if (result == "")
		{
			cout << "NULL" << endl;
		}
		else
		{
			cout << result << endl;

		}
	}


	return 0;
}
